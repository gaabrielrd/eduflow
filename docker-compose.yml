services:
  postgres:
    image: postgres:17-alpine
    container_name: eduflow-postgres
    environment:
      POSTGRES_DB: eduflow
      POSTGRES_USER: eduflow
      POSTGRES_PASSWORD: eduflow
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - eduflow-local
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U eduflow -d eduflow"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: eduflow-redis
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - eduflow-local
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e
    container_name: eduflow-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: eduflow
      MINIO_ROOT_PASSWORD: eduflow123
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    networks:
      - eduflow-local

  minio-init:
    image: minio/mc:latest
    container_name: eduflow-minio-init
    depends_on:
      - minio
    entrypoint:
      - /bin/sh
      - -c
      - |
        until mc alias set local http://minio:9000 eduflow eduflow123; do
          echo "Waiting for MinIO..."
          sleep 2
        done
        mc mb --ignore-existing local/${STORAGE_BUCKET_NAME:-eduflow-media}
        mc anonymous set download local/${STORAGE_BUCKET_NAME:-eduflow-media}
    networks:
      - eduflow-local

networks:
  eduflow-local:
    name: eduflow-local

volumes:
  postgres_data:
  redis_data:
  minio_data:
